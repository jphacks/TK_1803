<%= form_with(model: web_session, local: true) do |form| %>
  <% if web_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_session.errors.count, "error") %> prohibited this web_session from being saved:</h2>

      <ul>
      <% web_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
